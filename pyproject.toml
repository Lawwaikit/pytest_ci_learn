[project]
name = "py_ci_learn"
version = "0.1.0"
dependencies = ["pytest", "pytest-cov", "flake8"]


[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
